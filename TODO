hide solution while calculating
show combinations tried with solution
add commas to combinations (during progress and solution)
change solution to be nice HTML instead of a text area
add real spinner to progress
set nice fonts

experiment with combining same-weighted enchants as a shortcut for complex jobs
